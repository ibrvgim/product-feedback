import styles from '../../styles/components/Logo.module.css';

function Logo() {
  return (
    <div className={styles.logo}>
      <h1>
        Mind<span>hub</span>
      </h1>
    </div>
  );
}

export default Logo;
